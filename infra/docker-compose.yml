
services:
  api:
    build:
      context: ..
      dockerfile: infra/Dockerfile
      args:
        - PORT=${PORT:-8080}
    image: api-in-a-box:v0.0.1
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    volumes:
      - ../target:/app/target
