
services:
  api:
    build:
      context: ..
      dockerfile: infra/Dockerfile
      args:
        - PORT=${PORT:-8080}
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    volumes:
      - ../target:/app/target